[build-system]
requires = ["maturin>=1.15,<2"]
build-backend = "maturin"

[project]
name = "PyO3Demo"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
module-name = "rust_core"